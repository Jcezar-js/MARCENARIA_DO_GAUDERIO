# API Landing Page — Setup (Linux)

Este README descreve os passos para configurar o ambiente de desenvolvimento desde a instalação do Node/npm até o MongoDB (mongod) em Linux, e como rodar o projeto localmente.

## Pré-requisitos
- Node.js (v16+ recomendado) e npm
- MongoDB (mongod)
- git (opcional)

## 1) Clonar / abrir o projeto
Se ainda não clonou:
```bash
git clone <seu-repo> && cd api-landing-page

